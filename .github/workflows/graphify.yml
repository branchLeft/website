name: graphify

on:
  push:
    branches: [main]
    paths-ignore:
      - 'graphify-out/**'
  workflow_dispatch: {}

jobs:
  graphify:
    permissions:
      contents: write
    uses: branchLeft/github-workflows/.github/workflows/graphify.yml@v1
    secrets: inherit
