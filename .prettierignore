# Repo-specific ignores only. The shared set — node_modules, build output,
# coverage, graphify-out, sibling worktrees, lockfiles, minified assets — lives
# in node_modules/@branchleft/prettier-config/prettierignore and is passed as a
# third --ignore-path. This file is named explicitly in the format scripts
# because passing any --ignore-path replaces Prettier's default rather than
# adding to it.

# Playwright output. Not in the shared set: this is the only repo that runs it.
test-results/
playwright-report/
blob-report/
